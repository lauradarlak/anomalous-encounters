<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Browse All Categories</h1>
  </div>
</div>

<% @categories.each do |category| %>
<div class="card">
  <div class="card-body">
    <h5 class="card-title"><%= link_to category.name, category_path(category.slug) %></h5>
    <p>Description/definition</p>
  
  </div>
</div>
<% end %>
