<div id="navbar">
  <div class="navigation-example" style="background-image:url('assets/img/bg7.jpg')">
    <!-- Navbar Primary  -->
    <nav class="navbar navbar-expand-lg bg-primary">
      <div class="container">
        <div class="navbar-translate">
          <%= link_to "Anomalous Encounters", root_path, :class => "navbar-brand" %>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#example-navbar-primary" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-bar bar1"></span>
            <span class="navbar-toggler-bar bar2"></span>
            <span class="navbar-toggler-bar bar3"></span>
          </button>
        </div>
        <div class="collapse navbar-collapse" id="example-navbar-primary">
          <ul class="navbar-nav ml-auto">
            <% if signed_in? && !current_user.display_name.nil?%>
              <li class="nav-link text-white">Welcome, <%= current_user.display_name %></li>
            <li class="nav-item">
              <%= link_to "My Encounters", user_encounters_path(current_user.display_name), :class => "nav-link btn btn-neutral" %>
            </li>
            <li class="nav-item">
                <%= link_to "Log Out", destroy_user_session_path, method: :delete, :class => "nav-link" %>
            </li>
            <% else %>
            <li class="nav-item">
              <%= link_to "Log In", new_user_session_path, :class => "nav-link btn btn-lg btn-neutral" %>
            </li>
            <li>
              <%= link_to "Sign Up", new_user_registration_path, :class => "nav-link btn btn-lg btn-info" %>
            </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <!-- End Navbar Primary -->
